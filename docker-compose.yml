version: "3.9"

services:
  db:
    image: docker.io/postgres:16-alpine
    environment:
      POSTGRES_USER: roomies
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD must be set}
      POSTGRES_DB: roomies
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U roomies -d roomies"]
      interval: 5s
      timeout: 5s
      retries: 10

  backend:
    build:
      context: src/backend
      dockerfile: Dockerfile
    environment:
      DATABASE_URL: ${DATABASE_URL:?DATABASE_URL must be set}
      PORT: "8080"
      APP_ENV: production
      JWT_SECRET: ${JWT_SECRET:?JWT_SECRET must be set}
      CORS_ALLOWED_ORIGINS: ${CORS_ALLOWED_ORIGINS:-http://localhost}
    ports:
      - "${ROOMIES_BACKEND_PORT:-8080}:8080"
    depends_on:
      db:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "-q", "-O", "/dev/null", "http://127.0.0.1:8080/healthz"]
      interval: 30s
      timeout: 5s
      retries: 3

  frontend:
    build:
      context: .
      dockerfile: src/app/Dockerfile
      args:
        ROOMIES_API_URL: ${ROOMIES_API_URL:-/api}
    volumes:
      - frontend_dist:/export
    depends_on:
      backend:
        condition: service_healthy
    command: ["/bin/sh", "-c", "cp -a /app/dist/. /export/ && tail -f /dev/null"]

  caddy:
    image: docker.io/caddy:2.8-alpine
    ports:
      - "${ROOMIES_HTTP_PORT:-80}:80"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile
      - frontend_dist:/usr/share/caddy/html
    depends_on:
      backend:
        condition: service_healthy
      frontend:
        condition: service_started

volumes:
  pgdata:
  frontend_dist:
